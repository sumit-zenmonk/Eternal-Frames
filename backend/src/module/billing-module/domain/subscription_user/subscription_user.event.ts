export enum SubscriptionUserPublishEventEnum {
    SUBSCRIPTION_USER_CREATED = 'subscription_user.created',
    SUBSCRIPTION_USER_DELETED = 'subscription_user.deleted',
    SUBSCRIPTION_USER_RENEWED = 'subscription_user.renewed',
}
